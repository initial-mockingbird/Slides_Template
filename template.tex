% =====================================================================
%  RETRO MAC OS BEAMER TEMPLATE
%  Style: classic System 7 / Platinum window chrome, rainbow apple logo,
%  checkbox-style bullets, and a "code editor" window panel.
%  Compile with pdflatex (or xelatex).
% =====================================================================
\documentclass{retrobeamer}

%\usepackage[T1]{fontenc}
%\usepackage[utf8]{inputenc}
\usepackage{tikz}
\usetikzlibrary{shadows,calc,shapes.callouts}
\usepackage{calc}
\usepackage{ifthen}
\usepackage{listings}
\usepackage{xcolor}
%\usepackage{helvet}
\usepackage{graphicx}
\usepackage{fontspec} 
\usepackage[backend=biber, style=numeric]{biblatex}
\addbibresource{references.bib} % Replace with your .bib file name


% =====================================================================
%  TEMPLATE SLIDES -- copy any block below and replace the placeholder
%  text with your own content. Each is labeled by "STYLE" so you can
%  find the layout you want quickly.
% =====================================================================
\begin{document}

% ---------------------------------------------------------------------
% STYLE 0 -- Section divider / cover slide (no chrome, no code)
% Use a PLAIN frame (no [fragile] needed here).
% ---------------------------------------------------------------------
\begin{frame}
  \macdivider{Your Section Title}{An optional subtitle or tagline}
\end{frame}

% ---------------------------------------------------------------------
% STYLE 0B -- Alternative title slide: a centered "About This Deck"
% Mac dialog box (with OK button) over a desktop dressed with a few
% icons. Great as the very first slide of a deck. Use a PLAIN frame.
% ---------------------------------------------------------------------
\begin{frame}
  \mactitledialog{Proof Assistant Demo}{A gentle introduction to Lean}
    {J. Appleseed --- Fall 2026}
    {\dosfont demo.exe }
\end{frame}

% ---------------------------------------------------------------------
% STYLE 1 -- Bullet list (left) + code window (right)
% This is the layout from the reference screenshot.
% ---------------------------------------------------------------------
\begin{frame}[fragile,t]
  \macslidetitle{LEAN: Proof Assistant Demo Template}
  \begin{columns}[T]
    \begin{column}{0.40\textwidth}
      \begin{itemize}
        \item Structural Example: Definition
        \item Key Axioms \& Lemmas
        \item Theorem Application Steps
        \item Formalization Discussion
      \end{itemize}
    \end{column}
    \begin{column}{0.62\textwidth}
\begin{macwindow}{Lean Editor.pro}{Monaco}
theorem add_assoc (n m k : N) : n + m + k = n + (m + k) :=
begin
  induction n with n' hn,
    rw add_zero,  -- Base Case: 0 + m + k = 0 + (m + k)
    rw zero_add,
    rw add_succ,  -- Inductive Case: succ n' + m + k = succ n' + (m + k)
    rw hn,        -- Use inductive hypothesis
    rw add_succ,  -- etc.
end
\end{macwindow}
    \end{column}
  \end{columns}
  \mactip{Press CMD+P to Verify.}
\end{frame}

% ---------------------------------------------------------------------
% STYLE 1 (blank copy) -- same layout, ready for your own content.
% Just overwrite the bracketed placeholders below.
% ---------------------------------------------------------------------
\begin{frame}[fragile,t]
  \macslidetitle{Your Slide Title Here}
  \begin{columns}[T]
    \begin{column}{0.40\textwidth}
      \begin{itemize}
        \item \Large Your first point
        \item \Large Your second point
        \item \Large Your third point
        \item \Large Your fourth point
      \end{itemize}
    \end{column}
    \begin{column}{0.62\textwidth}
\begin{macwindow}{filename.ext}{Monaco}
-- Replace this with your own code, pseudocode, or text.
-- The box wraps long lines automatically.
your_function(argument):
    return result
\end{macwindow}
    \end{column}
  \end{columns}
  \mactip{A short tip or callout goes here.}
\end{frame}

% ---------------------------------------------------------------------
% STYLE 2 -- Full-width code window, no bullet list
% ---------------------------------------------------------------------
\begin{frame}[fragile,t]
  \macslidetitle{LEAN: Complete Proof Walkthrough}
  \begin{center}
\begin{macwindow}{Lean Editor.pro}{Monaco}
theorem mul_comm (n m : N) : n * m = m * n :=
begin
  induction n with n' hn,
    rw zero_mul,      -- Base Case: 0 * m = m * 0
    rw mul_zero,
    rw succ_mul,      -- Inductive Case: succ n' * m = m * succ n'
    rw hn,
    rw mul_succ,
end
\end{macwindow}
  \end{center}
  \mactip{Press CMD+P to Verify.}
\end{frame}

% ---------------------------------------------------------------------
% STYLE 3 -- Bullets only, two columns, no code window (e.g. a recap
% or roadmap slide)
% ---------------------------------------------------------------------
\begin{frame}[fragile,t]
  \macslidetitle{LEAN: Course Roadmap}
  \vspace{0.2cm}
  \begin{columns}[T]
    \begin{column}{0.48\textwidth}
      \begin{itemize}
        \item \Large Week 1: Propositional Logic
        \item \Large Week 2: Natural Numbers \& Induction
        \item \Large Week 3: Lists \& Recursion
      \end{itemize}
    \end{column}
    \begin{column}{0.48\textwidth}
      \begin{itemize}
        \item \Large Week 4: Tactics Deep Dive
        \item \Large Week 5: Building a Library
        \item \Large Week 6: Final Project
      \end{itemize}
    \end{column}
  \end{columns}
  \mactip{No code today -- just planning.}
\end{frame}

% ---------------------------------------------------------------------
% STYLE 4 -- Two code windows side by side (before / after, or A / B)
% ---------------------------------------------------------------------
\begin{frame}[fragile,t]
  \macslidetitle{LEAN: Simplifying a Proof}
  \begin{columns}[T]
    \begin{column}{0.48\textwidth}
      \centering{\small\bfseries Before}\\[2pt]
\begin{macwindow}{proof\_v1.lean}{Monaco}
theorem add_zero'
  (n : N) : n + 0 = n :=
begin
  induction n with n' hn,
    refl,
    rw add_succ,
    rw hn,
end
\end{macwindow}
    \end{column}
    \begin{column}{0.48\textwidth}
      \centering{\small\bfseries After}\\[2pt]
\begin{macwindow}{proof\_v2.lean}{Monaco}
theorem add_zero'
  (n : N) : n + 0 = n :=
by induction n;
   simp [*, add_succ]
\end{macwindow}
    \end{column}
  \end{columns}
  \mactip{The simp tactic closes both cases at once.}
\end{frame}

% ---------------------------------------------------------------------
% STYLE 5 -- Non-code content in a window (macbox): notes, a quote,
% a definition, or a screenshot placeholder. No [fragile] required
% unless something else on the slide needs it.
% ---------------------------------------------------------------------
\begin{frame}[t]
  \macslidetitle{A Note, Definition, or Quote}
  \begin{macbox}{Notes.txt}{Monaco}
    \vspace{2pt}
    \Large
    Replace this paragraph with your own text. The \texttt{macbox}
    environment gives you the same window chrome as \texttt{macwindow},
    but typesets ordinary LaTeX -- paragraphs, lists, tables, images --
    instead of code.

    \vspace{4pt}
    \begin{itemize}
      \item Works with itemize, \textbf{bold}, and \textit{italic}
    \end{itemize}
  \end{macbox}
  \mactip{Swap this box for a table, image, or blockquote.}
\end{frame}

\begin{frame}[fragile,t]
  \macslidetitle{LEAN: Simplifying a Proof}
\begin{macwindow}{proof\_v1.lean}{Monaco}
theorem add_zero'
  (n : N) : n + 0 = n :=
begin
  induction n with n' hn,
    refl,
    rw add_succ,
    rw hn,
end
\end{macwindow}
  \mactip{The simp tactic closes both cases at once.}
\end{frame}

% ---------------------------------------------------------------------
% STYLE 6 -- Big statistics / metrics row (no window chrome)
% ---------------------------------------------------------------------
\begin{frame}[t]
  \macslidetitle{By the Numbers}
  \vspace{1.2cm}
  \begin{columns}[T]
    \begin{column}{0.33\textwidth}
      \macstat{128}{theorems proved}
    \end{column}
    \begin{column}{0.33\textwidth}
      \macstat{42}{lemmas reused}
    \end{column}
    \begin{column}{0.33\textwidth}
      \macstat{6}{weeks of work}
    \end{column}
  \end{columns}
  \mactip{Swap in your own three (or more) headline numbers.}
\end{frame}

% ---------------------------------------------------------------------
% STYLE 7 -- Bibliography
% ---------------------------------------------------------------------

\begin{frame}[t]
  \macslidetitle{References}
  Just for the citations \cite{texbook}\cite{latex:companion}\cite{latex2e}\cite{knuth:1984}
\end{frame}

\begin{frame}[t]
  \macslidetitle{References}
  \vspace{1.2cm}
  \printbibliography
\end{frame}

\end{document}

